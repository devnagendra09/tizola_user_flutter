import 'package:flutter_test/flutter_test.dart';
import 'package:tizola_user/main.dart';

void main() {
  testWidgets('App widget exists', (WidgetTester tester) async {
    expect(const TizolaApp(), isNotNull);
  });
}
